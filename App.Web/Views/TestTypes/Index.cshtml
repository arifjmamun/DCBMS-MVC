@model IEnumerable<App.Core.Models.EntityModels.TestType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TestTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TestTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TestTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TestTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TestTypeId })
        </td>
    </tr>
}

</table>
